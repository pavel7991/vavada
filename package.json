{
  "name": "vavada",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"sass --watch src/styles/styles.scss src/styles/styles.css\"",
    "watch:scss": "sass --watch src/styles/styles.scss src/styles/styles.css",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "sass": "^1.89.2",
    "vite": "^7.0.0"
  }
}
